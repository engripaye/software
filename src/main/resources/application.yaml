spring:
  application:
    name: software
server:
  port: 8080

  datasource:
    url: jdbc:postgresql://localhost:5432/software
    username:
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
